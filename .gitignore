# Node / build
node_modules/
.next/
out/
dist/

# Env
.env
.env.local
.env.*.local

# LPOS runtime payloads
runtime/whisper-runtime/**
runtime/whisper-models/**
runtime/atem-bridge/*.exe
runtime/*.log
runtime/*-out.log
runtime/*-err.log

# External vendor SDKs and binaries
vendor/

# Local app state and generated data
data/

# Native build output
native/sony-camera-bridge/build/

# Local certificates
localhost+*.pem
localhost+*-key.pem

# TypeScript build info
*.tsbuildinfo

# Keep lightweight runtime scaffolding
!runtime/README.md
!runtime/whisper-runtime/.gitkeep
!runtime/whisper-models/.gitkeep
!runtime/atem-bridge/atem-bridge.js
