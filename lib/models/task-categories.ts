/**
 * Starter categories for Platform tasks (Phase F2).
 *
 * F2 ships these as a hardcoded constant so the New Task modal has a populated
 * dropdown immediately. F3 replaces this with a workspace-level store that
 * supports add / rename / reorder / remove via an admin UI — at which point this
 * file becomes the seed data for fresh installs and can be deleted from the
 * runtime read path.
 */

export const STARTER_PLATFORM_CATEGORIES: string[] = [
  'Pass Build',
  'Registration/Sales Page',
  'Workbooks',
  'Photos',
  'Misc',
];
