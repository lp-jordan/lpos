import { SilentPage } from '@/components/silent/SilentPage';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Produce' };

export default function SilentProducePage() {
  return <SilentPage slug="produce" />;
}
